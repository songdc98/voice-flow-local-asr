# Local voice trigger templates

This directory is for local wake-word templates.

Run:

```bash
.venv/bin/python voice_flow.py --enroll-voice-trigger
```

The generated `.npy` template files are ignored by Git because they are derived
from the user's voice.
